# Tinifier
URL shortener.

## Running with Docker
```sh
docker compose up -d
```
By default, it will be hosted on `*:8080`, but you can change the port in the `docker-compose.yml` file.

## License
[MIT](LICENSE).
